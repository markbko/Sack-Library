Implements a vector class template List and its associated iterators, with the same interface and iterator usage as that found in the C++ STL. Then that library is 
used for infix to postfix transformation.
